import {createTheme} from "@mui/material";

export default createTheme({
    palette: {
        mode: 'dark',
        primary: {
            main: '#90caf9',
        },
        secondary: {
            main: '#f48fb1',
        }
    },
    shape: {
        borderRadius: 10,
    }
})